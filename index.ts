/**
 * marketing-agentic-ai
 *
 * An agentic AI system of 26 specialized marketing agents that collectively
 * function as a full marketing team — powered by Claude.
 *
 * This is the future entry point for the orchestration layer.
 * Currently, the system runs via Claude Code CLI skill invocations.
 */
export const VERSION = "0.1.0";
